# requirements.txt
# Python dependencies required by the deployed Streamlit helipad detector app.
# Streamlit Community Cloud uses this file to build the app's Python environment
# via `pip install -r requirements.txt`. It should contain ONLY the packages
# actually imported and used by the Streamlit app in production.
#
# Location:
# - Recommended: repository root, or same directory as the Streamlit entrypoint
#   (e.g. apps/streamlit_app/app.py).
# - Name must be exactly `requirements.txt` so Streamlit Cloud detects it
#   automatically.

########################
# Core AI / YOLO
########################
ultralytics>=8.4.73           # YOLO models and utilities (Ultralytics YOLOv8)
torch>=1.8.0                  # PyTorch core for model execution
torchvision>=0.9.0            # Vision utilities, model zoo (used with YOLO)
tqdm>=4.64.0                  # Progress bars for long-running tasks
pyyaml>=5.3.1                 # YAML config parsing (model/settings files)
scipy>=1.4.1                  # Scientific utilities used by some YOLO tools

########################
# Scientific / analysis
########################
numpy>=1.22.2                 # Core numerical computing (arrays, math)
pandas>=1.1.4                 # Tabular data handling and analysis
matplotlib>=3.3.0             # Plotting (if you render static charts)
seaborn>=0.11.0               # Statistical visualization on top of Matplotlib

########################
# Imaging and vision
########################
pillow>=7.1.2                 # Image loading and basic image processing (PIL)
opencv-python-headless>=4.6.0 # OpenCV without GUI; avoids libGL display issues
                              # and is recommended for headless/cloud deployments.

########################
# Web app / UI
########################
streamlit>=1.42.0             # Web app framework used for the helipad detector UI
plotly>=5.18.0                # Interactive charts/plots inside Streamlit (optional)

########################
# General utilities
########################
requests>=2.23.0              # HTTP requests (API calls, file downloads)
psutil>=5.9.0                 # System resource monitoring (CPU/RAM usage)
py-cpuinfo>=9.0.0             # CPU information (useful for diagnostics in the app)
